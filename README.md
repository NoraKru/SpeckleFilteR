# SpeckleFilteR

'SpeckleFilteR' is an R package for applying and evaluating speckle filters on images or datasets. It provides several popular filters and metrics to help you select the best filter for your data.

## Installation
You can install the package directly from GitHub:

```r
# install.packages("devtools") if not already installed
remotes::install_github("NoraKru/SpeckleFilteR")

