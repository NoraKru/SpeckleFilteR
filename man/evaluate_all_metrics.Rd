% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_all_metrics.R
\name{evaluate_all_metrics}
\alias{evaluate_all_metrics}
\title{Evaluate Multiple Speckle Filter Quality Metrics}
\usage{
evaluate_all_metrics(image, filtered_image)
}
\arguments{
\item{image}{Original image (matrix or raster)}

\item{filtered_image}{A single filtered image or a named list of filtered images}
}
\value{
A data.frame with one row per filter and one column per metric
}
\description{
Computes several evaluation metrics (MSE, Average Difference, Speckle Index,
PSNR, SNR) for one or multiple filtered images.
}
\examples{
\dontrun{
img <- load_example()
filters <- all_filters(img, window_size = 3, ENL = 3.5, plot_result = FALSE)
metrics <- evaluate_all_metrics(img, filters)
# Ranked table of evaluation metrics
print(results$metrics)

# Short textual summary
cat(results$summary)
}
}
